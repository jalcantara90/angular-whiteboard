import { WhiteboardDirective } from './whiteboard.directive';

describe('WhiteboardDirective', () => {
  it('should create an instance', () => {
    const directive = new WhiteboardDirective();
    expect(directive).toBeTruthy();
  });
});
